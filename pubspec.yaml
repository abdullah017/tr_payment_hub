name: tr_payment_hub
description: >
  Turkish payment gateway integration (iyzico, PayTR, Param, Sipay).
  Supports proxy mode (Flutter + any backend) and direct mode (Dart backend).
version: 3.2.0
homepage: https://github.com/abdullah017/tr_payment_hub
repository: https://github.com/abdullah017/tr_payment_hub
issue_tracker: https://github.com/abdullah017/tr_payment_hub/issues

topics:
  - payment
  - iyzico
  - paytr
  - turkey
  - e-commerce

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  crypto: ^3.0.3
  meta: ^1.11.0
  xml: ^6.5.0
  webview_flutter: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.25.6
  lints: ^6.0.0
  mocktail: ^1.0.0
